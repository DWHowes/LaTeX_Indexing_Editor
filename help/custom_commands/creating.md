# Creating a Custom LaTeX Command

*(placeholder — to be written)*

Covers: defining a custom indexing command (e.g. a `\newcommand` wrapper around `\index`) so entries written with it are recognized alongside plain `\index`.
