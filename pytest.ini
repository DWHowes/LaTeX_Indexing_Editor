[pytest]
pythonpath = .
testpaths = tests
qt_api = pyside6
markers =
    integration: boots the real, fully-wired application object graph (slower, one per test module ideally)
    gui_smoke: drives real widgets end-to-end
